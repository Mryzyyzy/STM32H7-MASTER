/**
 * ===========================================================================
 * 业务模块层 — 摄像头采集模块
 * ===========================================================================
 *
 * 摄像头任务：支持 RGB565+LCD 显示 和 JPEG+串口打印 两种模式
 * 后续可扩展图像分片并通过 SPI 发送给 ESP32 从机
 */
#pragma once

/* 摄像头主任务（初始化 LCD + OV5640 + 主循环显示） */
void Camera_Task(void *argument);

/* JPEG 测试任务：初始化 OV5640 JPEG 模式 + 串口打印帧数据 */
void Camera_JPEG_Task(void *argument);
