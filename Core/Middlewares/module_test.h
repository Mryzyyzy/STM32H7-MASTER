/**
 * ===========================================================================
 * 测试上行模块 — 10Hz 心跳计数器
 * ===========================================================================
 *
 * 运行即发数据，用于验证 SPI 框架。接入真实传感器后删除此文件，
 * 并按相同接口实现新模块。
 */
#pragma once
#include "scheduler_data.h"

/* 模块对象（已在 scheduler_data.c 注册表中注册） */
extern DataModule_t g_data_module_test;

/* 采集任务函数（10Hz） */
void Test_Task(void* argument);
