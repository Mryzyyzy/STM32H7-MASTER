/**
 * ===========================================================================
 * 应用调度层 — FreeRTOS 任务创建
 * ===========================================================================
 *
 * 在 MX_FREERTOS_Init() 中调用 OsTasks_Create()，
 * 统一创建所有 RTOS 对象（线程、信号量等）。
 */
#pragma once

void OsTasks_Create(void);
