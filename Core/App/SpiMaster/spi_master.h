/**
 * ===========================================================================
 * 应用调度层 — 任务函数实现
 * ===========================================================================
 *
 * 存放 SPI_Main_Task 主调度逻辑。
 * 各传感器模块的采集任务在模块文件中实现（如 module_test.c → Test_Task）。
 */
#pragma once

/* SPI 主调度任务（10ms 周期，STM32 绝对主控） */
void SPI_Main_Task(void* argument);
